class Solution:
    def largestAltitude(self, gain: List[int]) -> int:
        res = 0
        dummy = 0
        for num in gain:
            dummy = dummy + num
            res = max(res, dummy)
        return res
        
