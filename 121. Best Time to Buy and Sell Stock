class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        min_val = float('-inf')
        
        min_num = prices[0]

        for num in prices:
            if num < min_num:
                min_num = num
            min_val = max(min_val, num-min_num)

        return min_val
