class Solution:
    def romanToInt(self, s: str) -> int:
        #left < right: right-left
        #Roman numerals are usually written largest to smallest from left to right. ##### too complex
        # ref_dict = {'I':1, 'V':5, 'X':10, 'L':50, 'C':100, 'D':500, 'M':1000}
        # res = 0
        # if len(s) == 1:
        #     return ref_dict[s[0]]

        # i = 1
        # while i < (len(s)):

        #     if ref_dict[s[i]] > ref_dict[s[i-1]]:
               
        #         res += ref_dict[s[i]]-ref_dict[s[i-1]]
        #         i += 2

        #         if i == len(s):
        #             res+=ref_dict[s[i-1]]
        #     else:
        #         if i != len(s)-1: 
        #             res += ref_dict[s[i-1]]
        #             i += 1
        #         else: 
        #             res += ref_dict[s[i-1]] + ref_dict[s[i]]
        #             i += 1
        # return res
        ref = {'I':1, 'V':5, 'X':10, 'L':50, 'C':100, 'D':500, 'M':1000}
        total = 0
        for i in range(len(s) - 1):
            if ref[s[i]] < ref[s[i+1]]:
                total -= ref[s[i]]
            else:
                total += ref[s[i]]
        return total + ref[s[-1]]

            

